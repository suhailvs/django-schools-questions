What does {% include %} does?, It will include another template., It will include content from another template having the same templates defined., It is the same as {% extend %}., None of the above, 1
What would "Post.objects.filter(heading="Post 1")", It will print the first object which matched with heading "Post 1", It will print all objects, It will print all the objects which match the heading "Post 1", 3
Suppose you want to count the number of books in Django with "books = Book.objects.all()". Which implementation would be fastest?, Database level Implementation - books.count(), Python Implementation - len(books), Template Language Implementation - {{ books | length }}, None of the above, 1